# 기여 가이드

## 커밋 컨벤션

[Conventional Commits](https://www.conventionalcommits.org/) 규칙을 따릅니다.

### 형식

```
<타입>: <제목>

<본문 (선택)>

<푸터 (선택)>
```

### 타입

| 타입 | 설명 |
|---|---|
| `feat` | 새로운 기능 추가 |
| `fix` | 버그 수정 |
| `refactor` | 기능 변경 없는 코드 구조 개선 |
| `perf` | 성능 개선 |
| `test` | 테스트 코드 추가/수정 |
| `docs` | 문서 수정 |
| `chore` | 빌드, 설정, 의존성 등 기타 작업 |
| `style` | 포맷팅, 세미콜론 등 코드 의미에 영향 없는 변경 |

### 규칙

- 제목은 50자 이내, 마침표 없이 작성합니다.
- 제목은 한글로 작성하되, "무엇을 왜" 했는지 알 수 있게 씁니다.
- 하나의 커밋에는 하나의 논리적 변경만 담습니다.
- 관련 이슈가 있으면 푸터에 `Closes #이슈번호`를 적습니다.
- 필요 시 타입 뒤에 스코프를 붙일 수 있습니다. 예: `feat(auth): ...`.

### 예시

```
feat: 주문 취소 API 추가

- 결제 완료 상태의 주문만 취소 가능하도록 검증 추가
- 취소 시 재고 원복 처리

Closes #42
```
